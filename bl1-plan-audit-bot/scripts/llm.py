#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""LLM-контур BL-1 — модель из kimi.json ('model'; с 24.09.26 аудит тоже
на топовой kimi-k3, C-level выводы — на 'model_sponsor').

Качество результата важнее времени и стоимости: большой max_tokens,
длинный таймаут, retry с backoff.

LLM получает ТОЛЬКО факты детерминированного анализа (analytics.run_analysis)
и интерпретирует их. Нарушения не выдумывает (наследие AC-19 спеки).

Структура заключения — по лучшим практикам:
- Asana (status reports): health-тег в начале, инвертированная пирамида,
  next steps с владельцами;
- PMI (EVM, performance reporting): variance analysis «причина → влияние →
  корректирующее действие», RAG, разделение рисков и проблем;
- DCMA 14-point: качество расписания по семействам structure/realism/performance.

Конфиг: .credentials/kimi.json
"""

import json
from typing import Optional

from config import load_kimi_config

TIMEOUT = 300
# reasoning расходует max_tokens — запас покрывает и размышления, и ответ.
MAX_TOKENS = 16000
MAX_FACTS_CHARS = 60000

SYSTEM_PROMPT = """Ты — старший аудитор PMO (PMP, 15+ лет контроля ИТ-проектов).
Тебе передан ДЕТЕРМИНИРОВАННЫЙ анализ проектного плана (JSON):
- metrics — метрики задач (выполнено/в работе/не начато/просрочено);
- cpm — критический путь;
- compliance — нарушения стандарта оформления плана (поле title — суть,
  evidence — примеры задач); поля rule и ref — внутренние служебные коды,
  в тексте заключения их упоминать ЗАПРЕЩЕНО;
- schedule_health — проверки качества расписания (поле name — человеческое
  название, family: structure — логика сети задач, realism — реалистичность
  дат и длительностей, performance — исполнение; threshold — допустимая норма;
  status pass/fail/n/a); поле id (D-NN) — служебный код, в тексте не упоминать;
- evm — темп выполнения (pv_pct — сколько % объёма должно быть сделано
  к сегодня, ev_pct — сколько фактически сделано, spi = ev/pv);
- health — сводный статус (on_track/at_risk/off_track) с причинами;
- model_completeness — НЕ заполненные поля плана (связи, ответственные,
  затраты, колонки) с полем benefit (польза заполнения). Это НЕ замечания и
  НЕ проблемы: многие команды сознательно не ведут эти поля. Используй их
  ТОЛЬКО в разделе «## Рекомендации» — как советы, повышающие качество плана
  и точность аудита, с формулировкой пользы из benefit;
- diff (если есть) — изменения к предыдущей версии плана.

Аудитория отчёта — руководители и проектные менеджеры БЕЗ подготовки по
методологиям: они не знают аббревиатур SPI, BEI, DCMA, EVM. Каждую метрику,
которую ты используешь, объясняй в скобках простыми словами, например:
«план выполняется на 38 % от графика (SPI = 0.38) — работы идут примерно
в 2,6 раза медленнее запланированного».

Напиши аудиторское заключение на русском языке СТРОГО в этой структуре
(Markdown, заголовки ровно как указано):

## Резюме для руководства
Начни со статуса проекта (🟢/🟡/🔴 из health) и оценки плана по 10-балльной
шкале. 3–4 предложения: главный вывод, самое критичное, главное действие.
Инвертированная пирамида — самое важное первым.

## Ключевые проблемы
3–5 проблем по убыванию критичности. Каждую оформи так:
**Проблема.** Причина (из evidence) → влияние на сроки/проект → что делать.
Различай ПРОБЛЕМЫ (уже случились: просрочки, срывы базовых дат) и РИСКИ
(могут случиться: узкий критический путь, задачи без связей).

## Качество плана
Пройди по трём группам проверок из schedule_health:
1) Логика сети (structure) — связи между задачами, ограничения дат;
2) Реалистичность (realism) — длительности, резервы, валидность дат,
   назначение ответственных;
3) Исполнение (performance) — срыв базовых дат, доля выполненного к сегодня,
   критический путь.
Для каждой группы: 1–2 предложения — что сломано, насколько масштабно
(count/percent против нормы threshold) и почему это важно для управления
проектом. Проверки со статусом n/a не выдумывай — при необходимости укажи
одной строкой, что для полной проверки нужен исходный .mpp.

## Оформление плана против стандарта
Сгруппируй нарушения из compliance в 2–4 темы (не перечисляй сотни строк):
суть темы своими словами (по полю title), масштаб (count), 1–2 примера
из evidence. Служебные коды и номера пунктов стандарта НЕ называй.

## Динамика за период
Только если есть diff: что изменилось, тренд (улучшается/ухудшается),
знаковые сдвиги сроков. Если diff нет — раздел пропусти.

## Рекомендации
5–7 действий PM по убыванию приоритета. Формат каждого:
действие → зачем (на какой факт опирается, своими словами) → срок
(на этой неделе / за 2 недели / к следующему статусу). Действия должны
быть выполнимыми в MS Project/Excel, не абстрактными.

ЖЁСТКИЕ ПРАВИЛА:
- Используй ТОЛЬКО факты из переданного JSON. Нельзя выдумывать нарушения,
  задачи, даты, причины. Если данных не хватает для вывода — скажи об этом.
- ЗАПРЕЩЕНО упоминать: служебные коды правил (R-01, D-14 и т.п.), слова
  «Инструкция», «DCMA», «EVM», номера пунктов стандартов. Говори о сути.
- Аббревиатуры SPI и BEI допустимы, только если рядом дано пояснение
  простым языком.
- Пиши плотно, по-деловому, без воды и без общих советов уровня
  «улучшите планирование». Объём — до 1500 слов."""


SPONSOR_PROMPT = """Ты — доверенный советник спонсора проекта (C-level).
Тебе передан ДЕТЕРМИНИРОВАННЫЙ аудит проектного плана (JSON): metrics, cpm,
compliance, schedule_health, evm, health, diff (если есть). Возможно, ниже
будет КОНТЕКСТ ОТ ПРОЕКТНОГО МЕНЕДЖЕРА — бизнес-цели, критерии успеха,
приоритеты. Если контекста нет — раздел о бизнес-целях пропусти.

Напиши отчёт ДЛЯ СПОНСОРА на русском языке. Спонсор занят и не обязан
разбираться в методологиях: ЗАПРЕЩЕНЫ любые аббревиатуры и термины
(SPI, BEI, CPM, EVM, DCMA, критический путь, освоенный объём) без перевода
на бизнес-язык. Все цифры — в форме «сдвиг примерно N месяцев», «каждая
вторая задача», а не «0.38».

СТРОГАЯ структура (Markdown, заголовки ровно как указано), одна страница:

## Главное
3 строки: что происходит с проектом и что это значит для бизнес-результата.

## Связь с бизнес-целями
Только если есть контекст от PM: проект по-прежнему ведёт к заявленной цели?
Что из обещанной ценности под угрозой? Если контекста нет — раздел пропусти.

## Ключевые риски
Максимум 3 риска по убыванию. Каждый: **Риск.** → что это значит для бизнеса
→ масштаб словами (высокий/средний). Без жаргона.

## Прогноз
Качественный, БЕЗ ложной точности: «при текущем темпе завершение сдвинется
ориентировочно на N–M месяцев» или «сроки достижимы». Опирайся на темп
выполнения и просрочки из фактов, точную дату не называй.

## Что нужно от вас
ГЛАВНЫЙ раздел. 2–4 конкретных решения/действия спонсора: эскалации за
пределами полномочий PM, решения go/no-go, снятие препятствий, ресурсы.
Формат: действие → зачем → к какому сроку.

## Вера в план
Одна строка: насколько плану можно доверять (высокая/средняя/низкая)
+ одно предложение почему (покрытие связями, наличие базового плана,
доля просрочек).

ЖЁСТКИЕ ПРАВИЛА:
- Используй ТОЛЬКО факты из JSON и контекст от PM. Ничего не выдумывай.
- Тон: уважительный, прямой, без запугивания и без приукрашивания.
- Объём — до 450 слов. Это читают за 2 минуты."""


def _facts_text(facts: dict) -> str:
    """JSON фактов для LLM. Полные списки задач ('items') исключаем —
    они нужны только drill-down в боте, а не модели (экономия токенов)."""
    def strip_items(obj):
        if isinstance(obj, dict):
            return {k: strip_items(v) for k, v in obj.items() if k != 'items'}
        if isinstance(obj, list):
            return [strip_items(x) for x in obj]
        return obj
    text = json.dumps(strip_items(facts), ensure_ascii=False, indent=1,
                      default=str)
    if len(text) > MAX_FACTS_CHARS:
        text = text[:MAX_FACTS_CHARS] + '\n…(усечено)'
    return text


def _call_kimi(system_prompt: str, user_content: str,
               log_fn=print, model: Optional[str] = None) -> Optional[str]:
    """Один вызов Kimi с retry/backoff. Ответ или None при сбое.

    model=None — модель из kimi.json ('model'); для C-level выводов
    (спонсорский отчёт, дайджест) передаётся model_sponsor (24.09.26:
    «максимально умный» — kimi-k3, как Pro-режим BL-28).
    """
    cfg = load_kimi_config()
    if not cfg:
        log_fn('⚠️ kimi.json не настроен, LLM-анализ недоступен')
        return None
    try:
        import requests
    except ImportError:
        log_fn('⚠️ requests не установлен, LLM-анализ недоступен')
        return None

    base_url = cfg.get('base_url', 'https://api.moonshot.ai/v1').rstrip('/')
    payload = {
        'model': model or cfg.get('model', 'kimi-k2.6'),
        'messages': [
            {'role': 'system', 'content': system_prompt},
            {'role': 'user', 'content': user_content},
        ],
        # temperature не передаём: kimi-k2.x принимает только temperature=1.
        # thinking ВКЛЮЧЁН (качество > скорость): глубокий разбор фактов.
        'max_tokens': MAX_TOKENS,
    }

    import time
    # 429 (concurrency=1 на организацию, аккаунт общий с BL-6) требует
    # длинных пауз: до 6 попыток, до 60 сек между ними.
    for attempt in range(6):
        try:
            resp = requests.post(
                f'{base_url}/chat/completions',
                headers={'Authorization': f"Bearer {cfg['api_key']}",
                         'Content-Type': 'application/json'},
                json=payload,
                timeout=TIMEOUT,
            )
            if resp.status_code != 200:
                log_fn(f'⚠️ Kimi API вернул {resp.status_code}: {resp.text[:200]}')
                time.sleep(min(60, 15 * (attempt + 1))
                           if resp.status_code == 429 else 2 ** attempt)
                continue
            content = (resp.json().get('choices') or [{}])[0] \
                              .get('message', {}).get('content', '') or ''
            if content.strip():
                return content.strip()
            log_fn('⚠️ Kimi вернул пустой ответ, повторяю')
        except Exception as e:
            log_fn(f'⚠️ Ошибка вызова Kimi API: {e}')
            time.sleep(2 ** attempt)
    return None


def analyze_plan(facts: dict, user_comment: Optional[str] = None,
                 log_fn=print) -> Optional[str]:
    """Факты анализа → аудиторское заключение (Markdown) или None при сбое.

    user_comment (v1.1): акценты от пользователя — LLM обязан ответить на них
    отдельным разделом «## Ответ на ваш запрос» (добавляется последним).
    """
    prompt = SYSTEM_PROMPT
    user = 'Детерминированный анализ плана (JSON):\n' + _facts_text(facts)
    if user_comment:
        prompt += (
            '\n\nПОСЛЕ раздела «## Рекомендации» добавь финальный раздел '
            '«## Ответ на ваш запрос»: прямой, конкретный ответ на комментарий '
            'пользователя, опирающийся только на факты анализа. Если комментарий '
            'спрашивает о том, чего в данных нет, — честно скажи, что для ответа '
            'нужно, и что видно из имеющегося.')
        user += ('\n\nКОММЕНТАРИЙ ПОЛЬЗОВАТЕЛЯ (его акценты и интересы, '
                 'учитывай их во всём заключении):\n' + user_comment.strip())
    return _call_kimi(prompt, user, log_fn)


def sponsor_report(facts: dict, context: Optional[str] = None,
                   log_fn=print) -> Optional[str]:
    """Факты анализа (+ бизнес-контекст) → отчёт для спонсора (Markdown).

    Модель — model_sponsor из kimi.json (kimi-k3, «максимально умный»),
    при отсутствии ключа — общая model (k2.6)."""
    user = 'Детерминированный аудит плана (JSON):\n' + _facts_text(facts)
    if context:
        user += ('\n\nКОНТЕКСТ ОТ ПРОЕКТНОГО МЕНЕДЖЕРА (бизнес-цели, '
                 'критерии успеха, приоритеты, ожидаемые решения):\n'
                 + context.strip())
    cfg = load_kimi_config() or {}
    return _call_kimi(SPONSOR_PROMPT, user, log_fn,
                      model=cfg.get('model_sponsor'))


DIGEST_PROMPT = """Ты — доверенный советник спонсора проекта (C-level).
Тебе передан ДЕТЕРМИНИРОВАННЫЙ аудит проектного плана (JSON): metrics, cpm,
compliance, schedule_health, evm, health, а ниже — ДИНАМИКА к прошлому аудиту
(дельты ключевых метрик) и МЕТРИКИ РЕЕСТРА ПОРУЧЕНИЙ (доля закрытых в срок,
открытые просроченные). Спонсор занят: ЗАПРЕЩЕНЫ аббревиатуры и термины
(SPI, BEI, CPM, EVM, DCMA) без перевода на бизнес-язык.

Задача: выбери ТРИ самых важных решения, которые спонсору стоит принять
на этой неделе. Опирайся только на переданные факты и динамику: приоритет —
ухудшающиеся показатели, массовые просрочки, срывы базовых дат, риски
критического пути, отставание поручений.

Верни СТРОГО JSON-массив из трёх объектов (без markdown-огород, без текста
вокруг):
[{"title": "короткое название решения (до 10 слов)",
  "why": "почему сейчас: 1–2 предложения с цифрами из фактов, простым языком",
  "decision": "что конкретно решить/сделать: 1 предложение с кем и к какому сроку"}]

ЖЁСТКИЕ ПРАВИЛА:
- Только факты из переданных данных. Ничего не выдумывай.
- Если данных хватает меньше чем на три решения — верни меньше (но минимум 1).
- Пиши плотно, по-деловому."""


def digest_decisions(facts: dict, trend: dict, bl6: Optional[dict],
                     log_fn=print) -> Optional[list]:
    """Факты + динамика + метрики поручений → до 3 решений для спонсора.

    Возвращает список dict {title, why, decision} или None при сбое.
    """
    user = 'Детерминированный аудит плана (JSON):\n' + _facts_text(facts)
    if trend and trend.get('available'):
        user += ('\n\nДИНАМИКА к прошлому аудиту (от ' +
                 str(trend.get('prev_date')) + '):\n' +
                 '\n'.join(trend.get('lines', [])))
    else:
        user += '\n\nДИНАМИКА: предыдущий аудит отсутствует — сравнивать не с чем.'
    if bl6 and bl6.get('available'):
        pct = bl6.get('done_on_time_pct')
        user += ('\n\nМЕТРИКИ РЕЕСТРА ПОРУЧЕНИЙ (на ' + str(bl6.get('as_of')) + '):\n'
                 f"всего поручений: {bl6['total']} · открыто: {bl6['open']} · "
                 f"выполнено: {bl6['done']} · отменено: {bl6['canceled']}\n"
                 f"закрыто в срок: {bl6['done_on_time']} из "
                 f"{bl6['done_on_time'] + bl6['done_late']}"
                 + (f" ({pct} %)" if pct is not None else
                    ' (у остальных нет даты закрытия — не считаем)') + "\n"
                 f"открытые просроченные: {bl6['open_overdue']}")
    else:
        user += '\n\nМЕТРИКИ РЕЕСТРА ПОРУЧЕНИЙ: недоступны.'
    cfg = load_kimi_config() or {}
    raw = _call_kimi(DIGEST_PROMPT, user, log_fn,
                     model=cfg.get('model_sponsor'))
    if not raw:
        return None
    try:
        text = raw.strip()
        if text.startswith('```'):
            text = text.split('\n', 1)[1].rsplit('```', 1)[0]
        data = json.loads(text)
        if isinstance(data, dict):
            data = [data]
        return [d for d in data if isinstance(d, dict) and d.get('title')]
    except Exception as e:
        log_fn(f'⚠️ не разобрал ответ дайджеста: {e}; сырой: {raw[:200]}')
        return None
