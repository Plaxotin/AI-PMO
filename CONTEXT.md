# AI PMO — контекст для Kimi Work

Файл-памятка для быстрого восстановления контекста в любом новом чате/устройстве.
Прочитай этот файл целиком в начале сессии.

## Проект

AI PMO toolbox — набор фич вокруг Telegram-ботов и Google Sheets.
Бэклог и статусы — в Notion (страницы BL-N). Репо: github.com/Plaxotin/AI-PMO (main).

## Сервер (Timeweb)

- SSH: `ssh -i ~/.ssh/timeweb_aipmo -o BatchMode=yes root@195.133.14.151`
- Бот BL-6 живёт в `/opt/openclaw-server/skills/task-registry/scripts/`
  (bot_handler.py, task_manager.py, llm.py, commands.py, feedback.py)
- Сервис: `task-registry-bot.service`, лог `/var/log/task-registry-bot.log`
- Бот BL-28 живёт в `/opt/bl28-requirements-bot/` (bot.py, generate.py, шаблон v2),
  сервис `bl28-requirements-bot.service`
- Конфиги (не в репо!): `.credentials/` — config.json, telegram.json, kimi.json, user_mapping.json
- Плановый дайджест: systemd `plaxotin-task-digest.timer`, пн–пт 06:17 UTC (9:17 МСК)
- OpenClaw gateway на том же сервере: telegram-канал и telegram-плагин ВЫКЛЮЧЕНЫ
  (channels.telegram.enabled=false, plugins.entries.telegram.enabled=false) — не включать.

## Деплой-цикл BL-6

1. Правка в локальном клоне `bl6-task-notifier/v2/scripts/`
2. `python -m py_compile <file>`
3. `scp -i ~/.ssh/timeweb_aipmo <file> root@195.133.14.151:/opt/openclaw-server/skills/task-registry/scripts/`
4. `systemctl restart task-registry-bot`
5. Проверка: `ps aux | grep bot_handler | grep -v grep | wc -l` = 1
6. Коммит + push в main.

## BL-6 «Task notifier» (@Plaxotin_task_bot) — статус: Реализовано (MVP), актуально на 12.09.2026

- Реестры поручений в Google Sheets; маппинг колонок по ИМЕНАМ заголовков
  (COLUMN_SYNONYMS дублируется в bot_handler.py и task_manager.py — держать синхронно).
  Поле «Контрагент» (синонимы Компания/КА), «Проект» упразднён. Срок: Srok korr приоритетнее Srok plan.
  Колонка «Приоритет» (Prioritet): шкала 0–3 (0 — критичный … 3 — низкий, по умолчанию 2);
  LLM проставляет при создании, команда «приоритет #N <0-3>» для правки.
- ID поручения пишется ЧИСЛОМ (сортировка/фильтр в Sheets); 12.09 проведена миграция
  существующих ID из текста в числа (value_input_option=USER_ENTERED).
- Вкладка «Контакты» (ФИО | Telegram | Компания) — источник логинов и компаний;
  АВТОПОПОЛНЯЕТСЯ ботом (ensure_contact): новые ФИО/логины/компании дописываются
  при создании поручения и при привязке логинов (admin_mode_bind и «Проверить»→привязки).
- Ответственный в реестре — ЕДИНЫЙ формат «Фамилия И.» (normalize_assignee_format):
  по логину/ФИО ищется в «Контактах» и берётся короткая форма; ники TG — только в «Контактах».
- Контрагент при создании = компания ответственного из «Контактов» (автоподстановка,
  в т.ч. в предпросмотре — функция enrich_create_command).
- Матчинг имён нечёткий (_person_matches_fio / _surname_match): склонения («Плахотина»),
  инициалы с любой стороны («К. Плахотин», «Плахотин К»), опечатки (Левенштейн ≤ 2).
  Инициал, если указан, обязан совпасть — иначе бот спрашивает логин.
- При создании поручения бот проверяет привязку ответственного к TG-логину;
  если нет — спрашивает админа перед записью (можно «⏭ Пропустить», валидация формата логина).
- Проверка дублей: символьная (find_recent_duplicate, 10 мин, блокирует) +
  СЕМАНТИЧЕСКАЯ через LLM (find_semantic_duplicate): при создании — предупреждение
  «возможный дубль #N» (не блокирует); в кнопке «Проверить» — секция «Возможные
  дубли по смыслу» по всем открытым (find_semantic_duplicate_pairs, llm.call_kimi_json).
- Роли: user / admin. Суперадмин упразднён, доступ к бэку только через Kimi Work или консоль.
  Админ может закрывать и править ЛЮБЫЕ поручения (проверка «своё/чужое» — только для user).
- Статус «Новое» упразднён: все поручения через бота создаются со статусом «В работе».
- LLM-режим (кнопка «Редактировать»): свободный текст → канонические команды (мультикоманды),
  предпросмотр с кнопками Да / ✏️ Изменить / Нет («Изменить» — правка фразой, ре-интерпретация
  с учётом исходного запроса), после «Да» — выполнение и выход из режима.
- LLM: kimi-k2.6; temperature не передаём; для перевода команд thinking отключён,
  для совета дайджеста — включён (max_tokens=8000, timeout=120).
- Дайджест: открытые поручения со сроком ≤ 14 дней, сортировка по дате, дни просрочки,
  дата в заголовке; плановый — с советом LLM; кнопка «Дайджест» — выбор чата.
- Удаление предыдущего дайджеста: send_telegram сохраняет message_id в
  `.credentials/last_digest.json` ({chat_id: [ids]}) и удаляет прошлую версию при новой отправке.
  Бот в групповых чатах — участник (не админ): удаляет только свои сообщения и только
  младше 48 часов (пятничный дайджест в понедельник не удалится — известное ограничение).
- Антиповтор кнопки «Дайджест»: флаг `_digest_running` (только пока реально формируется),
  НЕ фиксированные 240 сек.
- Авто-миграция chat_id: при 400 migrate_to_chat_id (группа → супергруппа) бот сам
  переписывает telegram.json (_migrate_chat_id). Чаты рассылки: toolbox = -1004450977548
  (супергруппа!), МТИ&PSI = -1002041861875.
- Переключатель «⏰ Авто-дайджесты» (dg_auto:toggle) гейтит плановые рассылки.
- История изменений реестра — вкладка «Лог» в Google Sheet (дата/время, инициатор, действие, ID, детали).

## BL-28 «Генератор требований к проекту» (@PMO_requirements_bot) — статус: MVP реализовано (06.09.2026)

- Локально: `bl28-requirements-generator/`; сервер: `/opt/bl28-requirements-bot/`,
  сервис `bl28-requirements-bot.service`. Деплой-цикл как у BL-6 (py_compile → scp → restart).
- Пайплайн: исходники (.docx/.txt/.md файлами и текстом в чат) → `generate.py` (kimi) →
  JSON по схеме → `fill_docx` в шаблон `project-requirements-template-v2.docx`
  (генерируется `create_template_v2.py`, структура — `parse_template.py` → template-structure-v2.json).
- Два режима: ⚡ Быстрый — бесплатно, kimi-k2.6, один вызов + thinking, 2–3 мин (~$0.05);
  💎 Pro — kimi-k3, посекционно (6 групп) + проход критика, 10–15 мин (~$0.62);
  бесплатный Pro-прогон: запрос админу → `/grant <uid>`. Usage — `outputs/_usage.jsonl`.
- Себестоимость генерации (цены Moonshot 08.2026: k2.6 $0.95/$4.00, k3 $3.00/$15.00
  за 1M in/out): Быстрый ≈ $0.046 на документ (замер Лензолото: 1.8k in / 11k out,
  6.2k reasoning); в проде подтверждено — среднее по 8 быстрым генерациям ≈ $0.056.
  Pro ≈ $0.62 (7 вызовов, 21k in / 37k out, 17k reasoning) — дороже в ~14 раз.
- Документ полностью на русском: приоритеты «Обязательно / Желательно / Возможно /
  Не входит», нумерация БТ/ФТ/НФТ, незаполненное — [уточнить].
- Название документа модель извлекает из исходников сама (заглушка «Проект» запрещена).
- Открытые вопросы — нумерованным сообщением в чат; ответы пользователя сохраняются
  в сессию (`data/<uid>/answers_NN.txt`, состояние — `data/<uid>/state.json`),
  кнопка «🔄 Перегенерировать с ответами» — без повторного списания Pro-квоты.
- Дневной лимит: 10 генераций на пользователя (первичные + перегенерации вместе),
  счётчик в `config.json` → `daily_gen.<дата МСК>.<uid>` (считается после успешного
  вызова LLM, старые дни чистятся).
- Кнопка «📄 Пустой шаблон» (callback template) — выдача шаблона без лимитов,
  также в ответе на /start. Сообщения короче 25 знаков (MIN_MATERIAL_CHARS) в
  материалы не пишутся (фильтр болтовни; короткие ответы на вопросы — можно).
- Описание бота (Bot API setMyDescription, ≤512 зн.) — `bot-description.txt`;
  инструкция пользователя — `user-guide.md` → `user-guide.docx` (create_user_guide.py);
  релиз-нотс для рассылки — `release-notes-bl28.md`.
- Известная особенность: версия v0.N считается по slug названия, а название может
  меняться между прогонами → нумерация может начаться заново с v0.1.
- Версии: каждый прогон = новый файл `v0.N` + строка в «Истории изменений» документа.
- Rate limit org Moonshot: вызовы последовательно, паузы 4 с, backoff; черновики секций
  в `outputs/_draft_<uid>.json` (resume), CLI-флаги `--sections-only` / `--from-draft`.
- Конфиги на сервере (не в репо!): `.credentials/` — telegram.json (token, owner_id),
  config.json (allowed_user_ids, pro_quota), kimi.json (модели kimi-k2.6 / kimi-k3).

## BL-1 «Аудит проектного плана» (@PMO_plan_auditor_bot) — статус: РЕАЛИЗОВАНО (07.09.26)

- Отдельный бот: сервер `/opt/plan-audit-bot/`, сервис `plan-audit-bot.service`,
  код в `bl1-plan-audit-bot/` (scripts/: bot_handler, config, plan_model, plan_parser,
  analytics, diff, llm, report, pdf, xlsx_export, state). Деплой-цикл как у BL-6.
- Рамка фичи и решения — в Notion, страница BL-1. Спека SPEC-PLAN-AUDIT.md — вторична (веб-версия не строим).
- Решения 06.09.26: .mpp в MVP (РЕАЛИЗОВАНО: default-jre-headless + pip jpype1/mpxj,
  JVM внутри процесса бота; маппинг MPXJ → Plan протестирован: связи, длительности,
  baseline, deadline, затраты, ресурсы); маскирования ПДн НЕТ; версии плана — в истории
  Telegram (бот держит только file_id в state.json); PDF без диаграмм D-01…D-04.
- Пайплайн: файл → parse_plan → run_analysis → LLM Kimi k2.6 (thinking ВКЛЮЧЁН,
  max_tokens=16000, timeout=300, ответ ~2,5 мин) → сводка в чат + PDF.
  Качество результата важнее времени и стоимости анализа.
- Вторая пользовательская функция (06.09.26): конвертация .mpp → Excel.
  На .mpp бот отвечает inline-кнопками «Аудит» / «В Excel» (callback_query);
  xlsx_export.py пишет заголовки из COLUMN_SYNONYMS + колонку «Тип»
  (round-trip точный: 632 задачи, 0 расхождений, включая вехи-сводки).
  После конвертации бот предлагает кнопку «Начать аудит» (и наоборот).
- Юнит-экономика (замер 07.09.26 на плане 632 задачи, тарифы api.moonshot.ai
  на 09.2026: $0,95/1M вход, $0,16/1M кэш, $4,00/1M выход; курс ~86 ₽/$):
  аудит ≈ $0,032 (~2,7 ₽) — 12 752 токена, из них thinking 4 507 (56 % затрат);
  худший случай ~$0,081 (~7 ₽). Конвертация ≈ 0 ₽ (без LLM, только CPU).
  Ориентир: 100 аудитов/мес ≈ 275 ₽.
- Аналитика (детерминированно, без LLM): CPM; правила Инструкции R-01…R-12;
  DCMA 14-point (адаптация D-01…D-14: логика, резервы, длительности, валидность дат,
  ресурсы, BEI, тест крит. пути; leads/lags/типы связей/ограничения — из .mpp);
  EVM (PV/EV/SPI, duration-weighted proxy при отсутствии затрат);
  сводный статус on_track / at_risk / off_track (RAG).
- Отчёт по практикам Asana/PMI: инвертированная пирамида, проблемы
  «причина → влияние → действие», рекомендации со сроками и ссылками на R-NN/D-NN.
- UX-редизайн отчёта 10.09.26 (по фидбеку прогона вживую; принято пользователем
  после повторной проверки в боте 10.09.26): все метрики
  объясняются простым языком прямо в тексте (SPI → «работы выполняются на 38 %
  от графика»; BEI → «из запланированных к сегодня выполнено 16 %»); строка
  статусов с подписями (Выполнено/В работе/Не начато); раздел DCMA →
  «Качество плана» с легендой «норма ≤ 5 %»; коды R-NN/D-NN и упоминания
  корпоративной Инструкции из вывода УБРАНЫ (правила работают только внутри
  анализа, наружу — человеческие формулировки RULE_TITLES); PDF перестроен под
  «понять за 5 минут»: статус-баннер → «План в цифрах» (колонка «Как читать»)
  → заключение LLM → приложение с детальными таблицами.
- LLM работает только с фактами аналитики — нарушения не выдумывает.
- e2e на реальном плане (План_график_АСУП_техдолг_18_06_26.mpp, 632 задачи):
  парсер → аналитика → LLM → PDF — полный прогон зелёный; пойман и исправлен
  ложноположительный R-07/R-08 по baseline для .mpp.
- TODO: CPLI полный расчёт; эталонный корпус планов для регрессии качества.

### BL-1 v1.1 — РЕАЛИЗОВАНО и задеплоено 12.09.26 (решения от 12.09.26)

Реализация: `analytics.py` — kind='model' для R-01/R-06/R-08 и D-01/D-10
(исключены из compliance_score и статуса здоровья), блок
`facts['model_completeness']` с обоснованием пользы (MODEL_BENEFITS);
`llm.py` — analyze_plan(user_comment) + раздел «Ответ на ваш запрос»,
sponsor_report() с отдельным SPONSOR_PROMPT, общий _call_kimi с retry
до 6 попыток / до 60 с при 429 (concurrency=1 на организацию, аккаунт
общий с BL-6); `bot_handler.py` — мягкий шаг комментария (awaiting +
кнопка «⏩ Пропустить»), команда/кнопка «📄 Отчёт для спонсора» с
опциональными уточняющими вопросами, кэш last_run в памяти;
`pdf.py` — замечания/рекомендации разведены, generate_sponsor_pdf
(1 страница). e2e зелёный: аудит с комментарием (раздел «Ответ на ваш
запрос» отвечает адресно) + спонсорский отчёт с бизнес-контекстом.
Drill-down (фидбек 13.09.26): после отчёта кнопки «🔎 Качество /
Замечания / Рекомендации» — детальные сводки с ПОЛНЫМИ списками задач
по каждой находке (analytics хранит 'items' целиком; в LLM не уходят —
_strip_items в llm._facts_text). Длинные сводки (>4000 символов) уходят
.txt-файлом. R-06 теперь перечисляет задачи без веса, а не только сверку
суммы. Доработка 13.09.26: кнопки переименованы в действия («🔧 Улучшить
качество» / «🛠 Устранить замечания» / «💡 Учесть рекомендации»); в
детализации у каждой находки — блок «🔧 Как исправить» (HOWTO по R-NN/D-NN
в report.py); кэш аудита персистентный (state.last_run.json) — drill-down и
спонсорский отчёт работают после перезапуска бота («сессия не устаревает»).

Доработка 13.09.26 (вечер, фидбек после теста v1.1) — РЕАЛИЗОВАНО и задеплоено:

1. **Кнопки drill-down сокращены:** «🔧 Качество» / «🛠 Замечания» /
   «💡 Рекомендации» (длинные подписи не влезали на десктопе и мобайле);
   полные формулировки перенесены в текст сообщения «Что дальше?».
   callback_data не менялись.
2. **Отзывчивость бота:** жалоба «отвечает 10–15 с». Диагностика: сеть
   сервер→Telegram быстрая (~0,1–0,25 с на вызов); причина — однопоточный
   цикл: во время аудита (LLM 1–3 мин, ретраи 429) бот молчал на любые
   сообщения. Решение: тяжёлые обработчики (аудит / конвертация / спонсор)
   в фоновых потоках (`_dispatch`), защита от дублей по чату (`busy`),
   keep-alive `requests.Session`, все print с `flush=True` +
   `Environment=PYTHONUNBUFFERED=1` в systemd-юните, лог медленных вызовов
   API (>2 с). stdout ранее был block-buffered — журнал показывал ошибки
   пачками с задержкой.
3. **Логика аудита (analytics.py):**
   - D-06 «Резерв > 44 дней» удалена как замечание (порог 44 дня убран):
     завышенный резерв — симптом отсутствия связи с задачей-потребителем
     результата; покрыто рекомендацией D-01/R-01 (тексты усилены).
   - D-08 «Длительность > 30 дней» (порог 44→30) — не замечание, а
     рекомендация (kind='model', MODEL_CHECKS): декомпозиция ради контроля
     промежуточного прогресса.
   - D-09 разделена: D-09 «Невалидные даты (прогноз в прошлом)» +
     новая D-15 «Даты не указаны» (незавершённая задача без start/finish).
   - D-11 «Срыв базовых дат окончания» и BEI объединены: D-14 теперь
     kind='metric' (не строка проверки); в сводке чата, PDF-таблице
     «Качество плана» и drill-down «Качество» BEI выводится сразу после
     D-11 с расшифровкой словами: «Индекс выполнения базового плана (BEI)».
     Из сводной таблицы «План в цифрах» (PDF) строка BEI убрана как дубль.

### BL-1 v1.2 — спонсорский дайджест (24.09.26) — РЕАЛИЗОВАНО, задеплоено, e2e зелёный

Задел 21.09.26 (Notion BL-1, «дашборд здоровья для спонсора») → MVP варианта A:
только Telegram, строго по запросу, без авторассылок, без самодельного
health score (в развитие, ждёт BL-38).

- `state.py` — история аудитов `audit_history.jsonl` (снимок ключевых метрик
  каждого прогона, ротация 26 на чат). Тренд — Δ между последними двумя
  снимками: SPI, BEI, просрочки, готовность, качество оформления.
- `bl6_metrics.py` (новый) — read-only метрики активного реестра BL-6
  (проект МТИ&PSI, Google Sheets): открыто/выполнено/отменено, доля
  закрытых в срок (дата закрытия ≤ срока), открытые просроченные.
  Подключение — `.credentials/bl6_registry.json` (spreadsheet_id + путь к
  service account BL-6); без файла блок поручений пропускается.
- `llm.digest_decisions()` — LLM-прогон (k2.6, thinking): «3 решения на
  неделю» для спонсора {title, why, decision} из фактов + тренда + BL-6.
- `report.build_sponsor_digest()` + `pdf.generate_digest_pdf()` — дайджест
  в чат и 1 страница A4 (баннер RAG, плитки KPI, динамика, поручения,
  3 решения).
- `bot_handler.py` — кнопка «📊 Дайджест для спонсора» после аудита +
  команда /digest; в run_audit добавлен state.snapshot_run().
- **Прямой путь (24.09.26, тот же день):** дайджест БЕЗ предварительного
  аудита — /digest без кэша → бот ждёт файл → parse + детерминированный
  анализ (LLM-аудит не нужен) → дайджест (~40 с total). Факты кэшируются
  как после аудита (drill-down/спонсорский отчёт доступны). e2e зелёный.
- Решения владельца 24.09.26: канал — только Telegram; авторассылка НЕ
  нужна; health score не делаем; реестр — МТИ&PSI.
- e2e на плане 632 задачи: аудит 53 с + дайджест 50 с, оба LLM-прогона
  зелёные; PDF проверен визуально.
- **LLM-режимы (24.09.26):** аудит — kimi-k2.6 (thinking вкл., max_tokens
  16000). C-level выводы (спонсорский отчёт и дайджест) — **kimi-k3**
  («максимально умный», как Pro у BL-28): ключ `model_sponsor` в kimi.json,
  fallback на `model`. Замер: 179 с, отчёт 2970 зн.; оценка ~$0,10 (~9 ₽)
  против ~3 ₽ у k2.6. Аудит на k3 НЕ переводили (вопрос стоимости/скорости).

### BL-1 v1.3 — лимиты + статистика, аудит на k3 (24.09.26) — РЕАЛИЗОВАНО, задеплоено

Бот открыт для всех в TG → введены бесплатные лимиты и учёт использования.

- `usage.py` (новый) — помесячная статистика (по МСК) в `usage.json`:
  у каждого chat_id — счётчики `audit`/`sponsor`. Лимиты: **1 аудит и 1
  спонсорский отчёт в месяц бесплатно**; владелец (107227641, + admin_ids
  из telegram.json) — безлимит. Дайджест и конвертация .mpp лимитам НЕ
  подлежат (прочтение формулировки владельца). Попытка списывается при
  старте прогона; при сбое сервиса (исключение / LLM недоступен) —
  возврат (refund), чтобы сбой не сжигал квоту.
- `bot_handler.py` — проверка лимита в начале `run_audit`/`run_sponsor`
  (до скачивания файла; при блоке — вежливое сообщение с датой сброса,
  первое число следующего месяца по МСК). Команда **/stats** — только для
  владельца: чаты, прогоны по месяцам (текущий + 2 предыдущих).
- **Аудит переведён на топовую kimi-k3** («делай аудит тоже на топовой
  модели»): `model` в kimi.json сервера = kimi-k3 (раньше k2.6); код не
  менялся — analyze_plan берёт `model`. `model_sponsor` оставлен как
  явный якорь для C-level выводов. **Замер k3-аудита: 256 с, 7091 зн.**
  (в пределах TIMEOUT=300); тексты бота честно говорят «2–5 минут».
- HELP_TEXT дополнен строкой про лимиты (прозрачность для публичного бота).
- Проверки: смоук usage.py (лимит/refund/безлимит владельца/независимость
  видов) + py_compile; интеграционный тест гейта на сервере (blocked-сообщение
  до скачивания, повторный список не происходит); commit 500232a → main.

### BL-1 v1.4 — единый C-level отчёт для спонсора + /myusage (25.09.26) — РЕАЛИЗОВАНО, задеплоено

Решение владельца 25.09.26: «объединить дайджест и отчёт спонсору — это же
по сути информация для C-level» + «добавь /myusage».

- **Единый «Отчёт для спонсора»**: дайджест v1.2 слит с отчётом v1.1 в один
  документ (PDF до 2 стр.): стр. 1 — аналитика/прогноз/решения спонсора от
  LLM (+ бизнес-контекст PM), стр. 2 — «Панель здоровья проекта»: KPI-плитки,
  динамика к прошлому аудиту, метрики поручений BL-6, 3 решения недели.
  Два LLM-прогона k3 под одним лимитом `sponsor` (~3–6 мин). Сбой второго
  прогона не роняет отчёт — PDF уходит без стр. 2.
- `pdf.py` — `generate_digest_pdf` удалён; `generate_sponsor_pdf` принимает
  `trend/bl6/decisions` (стр. 2 появляется только при наличии данных).
  `report.build_sponsor_digest` — чат-сводка к единому отчёту.
- **Флоу**: после аудита одна кнопка «📄 Отчёт для спонсора» → предложение
  бизнес-контекста → единый отчёт. Прямой путь без аудита: /sponsor без кэша
  → бот ждёт файл → метрики → предложение контекста → отчёт. Команда /digest
  и старые кнопки «digest» в истории чатов — алиасы в тот же флоу.
- `/myusage` — остаток попыток за месяц для любого пользователя
  (`usage.myusage_text`; владельцу — «безлимит»). HELP_TEXT обновлён.
- Проверки: смоук единого PDF (2 стр. с данными / 1 стр. без) локально с
  визуальной вычиткой рендеров; серверный e2e сухого прогона на реальных
  фактах/тренде/BL-6 (PDF = 2 стр.), гейт лимита до запроса файла;
  py_compile всех модулей; commit 931345d → main.

Решения, зафиксированные 12.09.26:

1. **Комментарий аудитору (мягкий шаг):** после загрузки файла бот предлагает
   в свободной форме указать акценты и интересы (кнопка «Пропустить» запускает
   аудит сразу). Комментарий идёт в промпт LLM; в отчёте появляется раздел
   «Ответ на ваш запрос».
2. **Проверки «полноты модели» → рекомендации, а не замечания:** связи
   предшественник/последователь (R-01), ответственные (D-10), затраты (R-06),
   состав колонок (R-08). Обоснование в тексте — польза для качества аудита
   («заполните связи — бот достоверно рассчитает критический путь»). На статус
   здоровья (RAG) не влияют, масштаб (count) по-прежнему показываем.
3. **Авто-правки плана ботом — ИСКЛЮЧЕНЫ из функционала** (решение 12.09.26,
   отменяет п.6 MVP-сценария v1.1 из рамки фичи).
4. **Новая фича — спонсорский отчёт (C-level), гибрид B+C:** отдельная команда
   бота; перед генерацией — опциональные уточняющие вопросы (цели компании,
   критерии успеха, приоритет срок/scope, ожидаемые от спонсора решения);
   язык — только русский. Источник методологии: PMI, Schibi & Lee 2015
   (роль спонсора: vision / governance / value). Шаблон — 1 страница A4,
   6 разделов: статус для бизнеса → главное (3 строки) → связь с бизнес-целями
   (при наличии контекста) → ключевые риски без жаргона (макс 3) → прогноз
   (качественный, без ложной точности даты) → «что нужно от вас» (эскалации,
   go/no-go — главный раздел) → «вера в план» (шкала реалистичности).
   Маппинг на факты: health/evm/compliance/schedule_health; себестоимость
   ≈ +1,5–2 ₽ за вызов (второй LLM-прогон по тем же фактам).

## Ключевые люди

- Константин @plaxotin (uid 107227641) — владелец, админ
- Админы BL-6: @LizaDav (uid 1147870132), Рената Плахотина (uid 5696238266)

## Как работать со мной (договорённости)

- Отвечать по-русски, коротко; bash-команды — с понятным description.
- Перед Edit всегда Read; деплой только после py_compile; в конце — отчёт что сделано/нет.
- Push в git иногда падает с schannel-ошибкой — просто повторить.
