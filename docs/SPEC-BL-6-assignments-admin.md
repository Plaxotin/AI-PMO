# BL-6 — Администратор поручений

**Версия:** 1.0 (MVP)  
**Дата:** 2026-05-24  
**Продукт:** AI PMO  
**Код бэклога:** BL-6 (Notion) / BL-1 (внутренняя нумерация репозитория)  
**WSJF:** 2 · **Волна:** 1 · **Сложность:** L · **Ценность:** Высокая  
**Связанные файлы реализации:** `docs/ASSIGNMENTS_ADMIN_CURSOR_PLAN.md`, `docs/BL1-0_KICKOFF.md`, `docs/BL1-0_VERIFICATION.md`

---

## 1. Цель и результат

Сервис фиксирует поручения из **Telegram** (текст и голос), ведёт **единый реестр по проекту** с минимальными обязательными полями: **описание, срок, ответственный**. При неполных данных сервис задаёт уточняющие вопросы. За настроенное время до дедлайна отправляет уведомление в тот же Telegram-канал. Актуальная версия реестра публикуется в открытый доступ с упором на доступность из РФ. Поддерживаются выгрузка и ручные корректировки с повторной публикацией.

**Убийственный сценарий для рынка РФ:** пользователь отправляет аудиозапись с летучки или созвона в Telegram-бота — в веб-приложении AI PMO появляется структурированный список поручений с описаниями, сроками и ответственными. Это полноценный UX продукта, а не только публичный снапшот реестра.

---

## 2. Нефункциональные требования

| Область | Требование |
|---------|------------|
| Инфраструктура | Вычислительный контур и хранение данных реестра — **на территории РФ** или нейтральный managed Postgres без блокировок (первичное хранение поручений и персданных). |
| Трансграничная передача | Для MVP допускается обращение к LLM-провайдерам вне РФ для извлечения полей (slot-filling); цель v2 — локальный или Cloud.ru Foundation Model. Явная политика минимизации данных, уходящих во внешний вызов. |
| STT-провайдер | Приоритет — **Yandex SpeechKit** или совместимый STT-сервис с хранением в РФ. Fallback-провайдер (Whisper-совместимый API) — только если политика данных организации разрешает. Выбор фиксируется в конфигурации проекта. |
| Публичный реестр | Хостинг статики (JSON + HTML) — площадка без блокировок в РФ (Yandex Object Storage + CDN, VK Cloud, Selectel, Timeweb и аналоги). Без зарубежных runtime-зависимостей на стороне читателя. |
| Доступность | Веб-приложение AI PMO — цель SLA 99.5% в бизнес-часы; публичный снапшот — статика CDN. |
| Идемпотентность | Ключ по `chat_id` + `message_id`; повторная доставка вебхука не создаёт дубль. |
| Персональные данные | В публичном слое — только согласованные колонки (минимизация PII). Полные данные — только в защищённом контуре. |
| Наблюдаемость | Структурированные логи (JSON), rate limit на webhook endpoint, секреты только через env-переменные. |
| Безопасность | Верификация `X-Telegram-Bot-Api-Secret-Token` на webhook. Signed links для ручных правок. Антидубли напоминаний через `ReminderLog`. |

---

## 3. Объём MVP

### 3.1 Обязательно в MVP

1. **Модель поручения:** `id`, `project_id`, `title`, `description` (опц.), `status` (enum: `draft` | `open` | `done` | `cancelled`), `due_at` (опц.), `assignee` (Telegram @username / user_id / отображаемое имя), `source` (`manual` | `import` | `webhook`), `created_at`, `updated_at`, `history`.
2. **Приём из Telegram:** текст и голос из привязанного канала / супергруппы; webhook с верификацией токена.
3. **STT для голоса:** загрузка voice-файла → STT → нормализация текста → тот же pipeline, что и текст.
4. **Ключевой сценарий «голос с мероприятия»:** аудио (запись летучки/созвона) → бот → структурированный список поручений в веб-UI AI PMO.
5. **Уточняющие вопросы:** диалог в ЛС с ботом или в треде; до заполнения трёх обязательных полей запись остаётся черновиком (`draft`).
6. **Уведомления в канал:** сообщение с описанием, сроком, ответственным и ссылкой на публичную строку реестра; антидубли.
7. **Публичный реестр:** статический снапшот JSON + HTML-таблица на RU-friendly CDN; версионирование `updated_at` / ETag.
8. **CRUD в веб-UI AI PMO:** список, карточка, форма создания/редактирования; фильтры по статусу, сроку, ответственному.
9. **Выгрузка и ручные правки:** экспорт CSV/XLSX; импорт с валидацией и отчётом об ошибках; optimistic locking по `version`.
10. **Журнал событий:** кто и когда создал / изменил / закрыл поручение.

### 3.2 Вне первой итерации (бэклог)

- Несколько каналов и матрица ролей; SSO / корпоративный LDAP.
- Другие мессенджеры (Slack, e-mail).
- Автоматическое определение ответственного из LDAP/HR по @username.
- Сложная дедупликация поручений с LLM-сравнением семантики.
- Мобильное приложение / PWA.

---

## 4. Функциональные требования

### 4.1 Ввод из Telegram (текст и голос)

- Сообщения из привязанного канала/супергруппы обрабатываются через **webhook** (Telegram Bot API).
- **Текст:** извлечение `description`, `due_at`, `assignee` — эвристики + LLM slot-filling при необходимости.
- **Голос:** скачать voice-файл → STT (приоритет провайдера, удобного для RU) → обработать как текст.
- **Ключевой сценарий «голос с мероприятия»:** пользователь пересылает в бота аудиозапись целого совещания; бот применяет STT и LLM-разбиение на отдельные поручения (каждое — отдельный черновик) → пользователь видит их в веб-UI AI PMO и подтверждает / редактирует.
- Минимальная запись **не создаётся в «боевом» реестре**, пока не заполнены три обязательных поля (`title`/`description`, `due_at`, `assignee`). До этого — черновик и сессия уточнений.

### 4.2 Уточняющие вопросы

- Диалог в **личке с ботом** или в **треде под исходным сообщением** (конфигурируется на уровне проекта), чтобы не засорять рабочий канал.
- Максимум раундов уточнений (например 3); таймаут черновика (например 24 ч) с автоотменой.
- После заполнения минимума — финализировать `Assignment`, статус меняется с `draft` на `open`; бот отправляет краткое подтверждение.
- LLM задаёт не более 1–3 вопросов за раунд, не переспрашивает поля с высоким confidence.

### 4.3 Уведомления в канал

- За N часов/дней **и/или** в день дедлайна — сообщение в тот же Telegram-канал.
- Антидубли: лог напоминаний `ReminderLog` по `(assignment_id, reminder_type)`.
- Текст уведомления: краткое описание, срок, ответственный, **ссылка на публичную строку реестра** (якорь/id).
- N (интервалы) и часовой пояс — конфигурируются на уровне проекта; можно отключить.

### 4.4 Публичный реестр (доступ из РФ)

- Публикация статического снапшота: **`registry.json`** + **`index.html`** (таблица, поиск на клиенте, без зарубежных runtime-зависимостей).
- Хостинг на площадке без блокировок в РФ (Yandex Object Storage + CDN, VK Cloud, Selectel, Timeweb и аналоги); международный mirror — опционально.
- Версионирование: `updated_at`, ETag для CDN-кэшей.
- Политика PII: в публичном слое только согласованные колонки.
- Обновление после каждой публикации ≤ 1–2 мин (или по кнопке «Опубликовать»).

### 4.5 Выгрузка и ручные правки

- **Экспорт:** CSV/XLSX полного реестра (или по проекту) с полем `updated_at`.
- **Импорт:** валидация, отчёт «принято / пропущено / конфликт», сопоставление по стабильному `id`; опционально diff-превью перед применением.
- **Ручные правки в UI:** форма редактирования с optimistic locking по `version`; защищённая страница или одноразовые signed links.
- После ручных правок — ре-публикация снапшота (автоматически или по кнопке).

---

## 5. Ключевые пользовательские сценарии

| ID | Сценарий |
|----|----------|
| US-A | Пользователь пишет текстовое поручение в канале → бот создаёт запись с описанием, сроком, ответственным. |
| US-B | Пользователь отправляет голосовое сообщение → STT → то же, что US-A. |
| US-C | **Убийственный сценарий:** пользователь пересылает запись летучки (аудио) → бот дробит на поручения → список в веб-UI AI PMO. |
| US-D | Поле не заполнено → бот задаёт уточняющие вопросы в тред/личку → после заполнения запись открывается. |
| US-E | Наступает срок → бот отправляет напоминание в канал со ссылкой на публичную строку реестра. |
| US-F | Менеджер открывает публичный URL реестра из браузера на РФ-IP → видит актуальную таблицу поручений. |
| US-G | Администратор выгружает XLSX, правит, загружает обратно → реестр обновлён и перепубликован. |

---

## 6. Архитектура и Cursor-агенты

Детализированный план реализации — `docs/ASSIGNMENTS_ADMIN_CURSOR_PLAN.md`. Ниже — сводка треков и фаз для работы в Cursor Cloud через агентов.

### Схема компонентов

```
Telegram (канал / личка) ──► Bot Webhook ──► Ingest + STT + LLM slot-filling
                                    │
                            ClarificationSession
                                    │
                              API + DB (Postgres)
                            ┌────────────────────────┐
                            │                        │
                  Scheduler (cron / outbox)    Public static generator
                  → напоминания в канал        → registry.json + HTML → CDN (РФ)
                                               ↑
                                    Import / Export / Admin UI
```

### Cursor-агенты (треки)

| Агент | Выход | Критерий готовности |
|-------|-------|---------------------|
| **A — Контракты и данные** | Zod-типы, SQL-миграции, seed | Миграции накатываются; типы согласованы |
| **B — Telegram: приём текста и голоса** | Webhook handler, STT | Сообщение из тестового канала → черновик в БД |
| **C — LLM slot-filling и уточнения** | Модуль `clarify`; состояние `ClarificationSession` | E2e-тест: неполное сообщение → вопросы → финальная запись |
| **D — Напоминания** | Worker/cron, `ReminderLog`, шаблоны | Тестовая запись получает пост в канал ровно один раз на интервал |
| **E — Публичный реестр** | `registry.json` + `index.html` + скрипт деплоя CDN | URL открывается из РФ; обновление ≤ 2 мин |
| **F — Выгрузка и ручные правки** | Экспорт CSV/XLSX; импорт с diff | Round-trip: экспорт → правка → импорт → снапшот обновлён |
| **G — Наблюдаемость и безопасность** | Структурированные логи, rate limit, чеклист угроз | Повторная доставка webhook не создаёт дубль; секреты только в env |

### Порядок слияния

1. **A** (схема) — параллельно с черновиком OpenAPI.
2. **B** + **C** — после A.
3. **D** — после B + C (нужны финальные записи).
4. **E** — после A (можно параллельно B с мок-данными).
5. **F** — после A и желательно E (общий формат снапшота).
6. **G** — непрерывно, финальный проход перед пилотом.

### Фазы BL-6 (внутренняя нумерация репозитория: BL1-*)

| Фаза | Описание | Статус |
|------|----------|--------|
| BL1-0 | Контракты, Zod-типы, миграции v1, скелет API | ✅ `verified` (см. `docs/BL1-0_VERIFICATION.md`) |
| BL1-1 | CRUD поручений, авторизация, журнал истории | `planned` |
| BL1-2 | UI реестра (список, карточка, форма, фильтры) | `planned` |
| BL1-3 | Telegram-инжест, STT, LLM slot-filling, уточнения | `planned` |
| BL1-4 | Напоминания (cron/outbox), публичный реестр (CDN), импорт/экспорт | `planned` |
| BL1-5 | Сценарий US-C «голос с мероприятия», наблюдаемость, пилот | `planned` |

### Рекомендуемые LLM по фазам

| Фаза | Режим Cursor | Модели |
|------|-------------|--------|
| BL1-0 (выполнено) | Agent / Composer | Claude Sonnet, GPT-4.1 |
| BL1-1 CRUD + authz | Agent | Sonnet, GPT-4o |
| BL1-2 UI реестра | Agent / Composer | Sonnet, GPT-4o |
| BL1-3 Telegram + STT + LLM | Agent + Chat | GPT-4.1 / Sonnet; промпты slot-filling — Opus точечно |
| BL1-4 Напоминания + CDN + импорт | Agent | Sonnet, GPT-4o |
| BL1-5 US-C + наблюдаемость | Agent + Chat | Sonnet; диагностика STT-качества — Opus |

---

## 7. Данные и конфиденциальность

**В LLM передаётся только:**

1. Нормализованный текст сообщения (после STT, без бинарных данных).
2. Текущий черновик поручения (только заполненные поля — для контекста уточнений).
3. Набор полей для извлечения (описание задачи slot-filling, без истории чата).

**В LLM не передаётся:**

- Полный лог переписки канала.
- Персональные данные, не относящиеся к конкретному поручению.
- Аудиофайл (передаётся только в STT-сервис).

**Публичный реестр:** только согласованные колонки; ответственный — @username или отображаемое имя по политике проекта. Никаких внутренних id пользователей, email, номеров телефонов.

**Хранение аудио:** временные файлы STT-обработки удаляются после успешной транскрипции (TTL ≤ 1 ч). Оригинальный аудиофайл не сохраняется в БД.

---

## 8. Настройка Cursor Agent Skills

| Шаг | Действие | Статус |
|-----|----------|--------|
| 1 | Project-level skill `agents-best-practices` в `.cursor/skills/` | ✅ Сделано |
| 2 | Триггер в `AGENTS.md` (harness / MCP / BL-1) | ✅ Сделано |
| 3 | Доменный skill `ai-pmo-assignments` | ✅ Сделано |
| 4 | Kickoff BL1-1: сеанс в Agent mode с `@agents-best-practices` + `@ai-pmo-assignments` → blueprint инструментов, permissions, launch gate | Следующий шаг |
| 5 | BL1-3: STT-провайдер (Yandex SpeechKit credentials) — секреты через Cursor Dashboard → Cloud Agents → Secrets | Перед стартом BL1-3 |

---

## 9. Контракт API (логический, MVP)

### Реестр поручений

```
GET    /api/projects/:projectId/assignments          — список с фильтрами
POST   /api/projects/:projectId/assignments          — создать поручение
GET    /api/projects/:projectId/assignments/:id      — карточка
PATCH  /api/projects/:projectId/assignments/:id      — обновить
DELETE /api/projects/:projectId/assignments/:id      — отменить (soft delete)
```

Фильтры списка: `status`, `due_before`, `due_after`, `assignee`, `source`, `page`, `per_page`.

Ответ: `{ data: Assignment[], meta: { total, page, per_page } }`.  
Версионирование схемы: заголовок `X-Api-Version` или query-параметр.

### Webhook Telegram

```
POST /api/telegram/webhook    — приём обновлений от Telegram Bot API
```

Верификация `X-Telegram-Bot-Api-Secret-Token`. Идемпотентный ключ: `chat_id` + `message_id`. Быстрый ответ 200 (обработка асинхронно).

### Публикация реестра

```
POST /api/projects/:projectId/publish    — запустить генерацию снапшота и деплой
GET  /api/projects/:projectId/registry   — текущий статус публикации (URL, updated_at, ETag)
```

### Экспорт / импорт

```
GET  /api/projects/:projectId/assignments/export?format=csv|xlsx
POST /api/projects/:projectId/assignments/import          — тело: multipart/form-data, поле file
```

Ответ импорта: `{ accepted: N, skipped: N, conflicts: [...] }`.

---

## 10. Ошибки и границы

- Голосовой файл **> 25 МБ** → отказ с подсказкой; STT-провайдер недоступен → `503` с retry-подсказкой.
- Вебхук без токена или с невалидным → `403`.
- Черновик без заполнения трёх обязательных полей за 24 ч → автоотмена (`cancelled`) с уведомлением в тред/личку.
- Импорт с невалидными строками → строки пропускаются; полный отчёт ошибок в ответе; успешные строки применяются.
- Конфликт версии при ручной правке → `409` с текущей версией объекта; клиент предлагает merge.
- Rate limit webhook: 100 обновлений/минуту на бота → `429` с `Retry-After`.

---

## 11. Критерии приёмки MVP (пилот)

1. **Текст в канале → запись** с описанием, сроком, ответственным (или цикл уточнений до заполнения).
2. **Голос → то же** (STT → slot-filling → черновик → открытое поручение).
3. **US-C «голос с мероприятия»:** аудио с совещания → список поручений в веб-UI AI PMO; пользователь подтверждает / корректирует.
4. **За N времени до `due_at`** — сообщение в тот же канал; повторная доставка не создаёт дубль.
5. **Публичный URL реестра** открывается из РФ; отражает последнюю опубликованную версию (≤ 2 мин после публикации).
6. **Выгрузка → правка → импорт → публикация** без расхождений с ожиданием.
7. **Уточняющие вопросы** при нехватке полей; таймаут черновика соблюдается.
8. Повторная доставка одного и того же webhook-сообщения не создаёт дубль в реестре.
9. Все секреты (токены бота, ключи STT, ключи CDN) хранятся только в env; нет в логах.

---

## 12. Открытые вопросы (не блокируют старт BL1-1)

| Вопрос | Кто решает | Когда нужен ответ |
|--------|-----------|-------------------|
| Конкретный STT-провайдер по умолчанию (Yandex SpeechKit vs альтернатива) | Tech lead | До старта BL1-3 |
| CDN-площадка для публичного реестра (Yandex Object Storage vs Selectel vs другое) | DevOps / Tech lead | До старта BL1-4 |
| Политика хранения аудиофайлов (TTL, маскирование в логах) | Product + юрист/ДПО | До старта BL1-3 |
| Диалог уточнений: личка или тред (дефолт для пилота) | Product | До старта BL1-3 |
| Авторизация ручных правок: admin-страница или signed links | Product | До старта BL1-4 |
| Сопоставление @username ↔ ФИО/сотрудник при разных форматах в одной команде | Product | До пилота |

---

*Документ является продуктовой спецификацией BL-6. Детализированный план реализации через Cursor-агентов — `docs/ASSIGNMENTS_ADMIN_CURSOR_PLAN.md`. Kickoff и зафиксированные допущения BL1-0 — `docs/BL1-0_KICKOFF.md`. Результаты верификации BL1-0 — `docs/BL1-0_VERIFICATION.md`.*
