{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "version": 2,
  "builds": [
    {
      "src": "index.html",
      "use": "@vercel/static"
    },
    {
      "src": "app/package.json",
      "use": "@vercel/next"
    }
  ],
  "routes": [
    { "src": "/app/api/(.*)", "dest": "/api/$1" },
    { "src": "/api/(.*)", "dest": "/api/$1" },
    { "src": "/assignments(.*)", "dest": "/assignments$1" },
    { "src": "/_next/(.*)", "dest": "/_next/$1" },
    { "src": "/favicon.ico", "dest": "/favicon.ico" },
    { "src": "/", "dest": "/index.html" },
    { "src": "/(.*)", "dest": "/index.html" }
  ],
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        { "key": "X-Content-Type-Options", "value": "nosniff" }
      ]
    }
  ]
}
